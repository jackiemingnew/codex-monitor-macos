# Codex Monitor 项目约定

本文件补充上级 `AGENTS.md`，仅适用于本仓库。

## App 开发交付闭环

- 凡修改会进入 App 可执行文件并影响功能、Bug、UI、运行行为或架构的代码，完成标准不止是测试或 `swift build` 通过。除非用户本次明确要求不安装、不启动或只做静态验证，否则必须继续完成：打包 App、安装正式副本、重新启动、真实运行验收。
- 使用仓库正式流程：先运行与风险相称的测试，再执行 `./scripts/build-app.sh`，随后执行 `./scripts/install-user-app.sh`。不得把裸 Swift 二进制、`dist`、`.build` 或临时目录中的 App 当作已安装版本启动或交付。
- 唯一正式安装路径是当前用户的 `${HOME}/Applications/codex监测.app`，执行前解析为该用户的绝对路径。临时包与回退包必须放在隐藏构建目录并使用 `.app.noindex`，避免 Spotlight 出现第二个“codex监测”；不得删除或改动 CodexBar 等其他应用。
- 覆盖安装前必须为现有正式 App 创建可恢复备份。安装过程应停止旧 `CodexNotch` 进程并启动新版本，避免新旧构建同时运行。
- 安装后至少验证：`codesign --verify --deep --strict` 通过、可执行文件架构正确、仅有一个 `CodexNotch` 进程且路径来自正式安装目录、与本次改动直接相关的真实 UI 或运行行为通过。HUD/UI 改动应使用正式安装版进行目标宽度视觉和辅助功能文本检查。
- 最后重新检查 `git status --short` 和 `git diff --check`，证明没有清理、覆盖或混入用户既有改动。打包、安装和启动不代表获得提交、推送、发布或修改其他应用的授权。
- 若环境或授权导致任一步无法完成，必须明确标记 `UNVERIFIED` 或 `PARTIAL`，说明缺失的安装或运行证据，不得仅凭源码、测试或构建成功宣称交付完成。
- 纯文档、测试夹具或不会进入 App 可执行文件的改动不要求重新打包安装，除非用户明确要求。
